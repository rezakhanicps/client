import { BrowserRouter, Route, Router } from 'react-router-dom';
const App = () => {
    return (
        <div>
            <BrowserRouter>
                {/* <Route path="/" exact component={} /> */}
            </BrowserRouter>
        </div>
    );
};

export default App;
